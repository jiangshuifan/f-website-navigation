export default {//有异步操作时，在这里写
    //context:上下文，这里对应store对象
    // action_updateName(context,payload){
    //     return new Promise((resolve,reject)=>{
    //         setTimeout(()=>{
    //             context.commit("updateName",{name:payload.name})//这里通过commit向mutations提交
    //           },1000)
    //           payload.sayHi();
    //         resolve("什么？")
    //     })
    // },
}